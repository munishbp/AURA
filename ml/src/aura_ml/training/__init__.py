"""Training: minimal `train.py` against `diffusers` + `peft` for LoRA fine-tuning
of Qwen-Image-Edit-2509 on (control, target, instruction) triples.

For real runs, prefer the ai-toolkit configs under `configs/` once parity has
been verified.
"""
