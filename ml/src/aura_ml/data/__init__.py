"""Data: PyTorch Dataset over (control, target, instruction) triples plus a
synthetic-pair generator used to bootstrap training data from unpaired sources.

See `SCHEMA.md` in this directory for the on-disk dataset format.
"""
